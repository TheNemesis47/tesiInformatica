[project]
name = "meta-quest-vision-monorepo"
version = "0.1.0"
description = "Monorepo per i progetti di tesi — vision-caption e object-memory"
authors = [{ name = "Samuel Arenella" }]
requires-python = ">=3.14"
dependencies = [
    "httpx>=0.28.1",
]

[tool.uv.workspace]
members = ["packages/*"]

[dependency-groups]
dev = [
    "ruff>=0.8.0",
    "mypy>=1.13.0",
    "pre-commit>=4.0.0",
]
